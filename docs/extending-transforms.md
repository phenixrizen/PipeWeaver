# Adding a transform

1. Add the transform case in `internal/mapping/applyTransform`.
2. Define any additional config fields on `mapping.Transform`.
3. Add backend tests for the transform.
4. Update the frontend transform selector options.
5. Document the transform in examples or README if it changes user-facing behavior.
